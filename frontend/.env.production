# Production environment for Azure Static Web Apps builds.
# These values are baked into the bundle by Vite at build time.
VITE_API_BASE_URL=https://scanberry-api.azurewebsites.net
VITE_USE_MOCK=false
